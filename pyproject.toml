[project]
name = "pomodoro-timer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["ttkbootstrap>=1.20.2"]

[dependency-groups]
dev = ["pyinstaller>=6.19.0", "pytest>=8.4.0"]
